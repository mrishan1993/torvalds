import { PartialTezosTransactionOperation } from '@airgap/beacon-sdk';
import { MessageSink, LoginResponse, OperationResponse, TrackResponse, LogoutResponse, AuthResponse, Success, SignExprResponse, Template, LoginConfig } from './types';
export declare class KukaiMessaging {
    #private;
    constructor(messageTarget: MessageSink, allowedOrigin: string, enableLogging: boolean);
    init(eventSrc: EventTarget): Promise<void>;
    get isInit(): boolean;
    deinit(): void;
    card(show: boolean): Promise<void>;
    login(config: LoginConfig): Promise<LoginResponse & Success>;
    operation(operations: PartialTezosTransactionOperation[], ui?: Template): Promise<OperationResponse & Success>;
    track(opHash: string): Promise<TrackResponse & Success>;
    logout(): Promise<LogoutResponse & Success>;
    signExpr(expr: string, ui?: Template): Promise<SignExprResponse & Success>;
    authenticate(id: string, nonce: string): Promise<AuthResponse & Success>;
    dismiss(): Promise<void>;
}
