import { DeferredPromise, Networks } from './types';
export declare const defer: <T>() => {
    deferred: DeferredPromise<T, Error>;
    promise: Promise<T>;
};
export declare const networkToSrc: (network: Networks | string) => string;
